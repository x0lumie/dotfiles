# #######################################################################################
# HYPRLAND CONFIGURATION - CLEAN BLUR & GLASS EFFECT
# #######################################################################################

monitor = DP-2, 1920x1080@144, 0x0, 1
monitor = , preferred, auto, 1

# --- Programs & Modifiers ---
$terminal    = kitty
$fileManager = nautilus
$menu        = fuzzel
$mainMod     = SUPER

# --- Autostart ---
exec-once = quickshell -c ~/.config/quickshell/noctalia-shell/
exec-once = systemctl --user start hyprpolkitagent.service

# --- Environment (NVIDIA) ---
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = NVD_BACKEND,direct
env = GBM_BACKEND,nvidia-drm
env = __GL_MaxFramesAllowed,1
env = WLR_NO_HARDWARE_CURSORS,1

env = XDG_MENU_PREFIX,arch-
env = QT_QPA_PLATFORMTHEME, kde
env = QT_STYLE_OVERRIDE, kvantum 

# --- Look and Feel ---
general {
    gaps_in = 12
    gaps_out = 20
    border_size = 2
    col.active_border = rgba(ffffffff) rgba(333131ff) 45deg
    col.inactive_border = rgba(59595966)
    layout = master
    resize_on_border = true
}

decoration {
    rounding = 2
    
    # Opacity: Active is mostly solid for focus, Inactive lets the BG show
    active_opacity = 0.98
    inactive_opacity = 0.93

    shadow {
        enabled = true
        range = 20
        render_power = 4
        color = rgba(0, 0, 0, 0.45)
    }

    blur {
        enabled = true
        size = 2     
        passes = 1
        new_optimizations = on
        ignore_opacity = on
        xray = false
        noise = 0.01
        contrast = 1.0
    }
}

# --- Smooth Animations ---
animations {
    enabled = yes
    bezier = fluent_decel, 0, 0.2, 0.4, 1
    bezier = easeOutQuint, 0.23, 1, 0.32, 1

    animation = windows, 1, 3, easeOutQuint, popin 80%
    animation = windowsOut, 1, 3, fluent_decel, popin 80%
    animation = workspaces, 1, 4, easeOutQuint, slide
    animation = fade, 1, 4, default
}

input {
    kb_layout = us
    follow_mouse = 1
    force_no_accel = true
}

dwindle {
    pseudotile = true
    preserve_split = true
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    vfr = true
}

# --- Keybindings ---
 # See https://wiki.hypr.land/Configuring/Keywords/

$mainMod = SUPER # Sets "Windows" key as main modifier


bind = , Print, exec, hyprshot -m region --clipboard-only

bind = $mainMod, V, exec, cliphist list | fuzzel --dmenu | cliphist decode | wl-copy

bind = $mainMod, period, exec, BEMOJI_PICKER_CMD="fuzzel --dmenu" bemoji

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more

bind = $mainMod, RETURN, exec, [float: center; size: 800 600] $terminal

bind = $mainMod, C, killactive,

bind = $mainMod, L, exit,

bind = $mainMod, E, exec, $fileManager

bind = $mainMod, T, togglefloating,
bind = $mainMod, T, resizeactive, exact 800 500

bind = $mainMod, Q, exec, $menu

bind = $mainMod, P, pseudo, # dwindle

bind = $mainMod, J, togglesplit, # dwindle

bind = $mainMod, M, fullscreen, 1

bind = $mainMod, F11, fullscreen, 0


# Move focus with mainMod + arrow keys

bind = $mainMod, left, movefocus, l

bind = $mainMod, right, movefocus, r

bind = $mainMod, up, movefocus, u

bind = $mainMod, down, movefocus, d


# Switch workspaces with mainMod + [0-9]

bind = $mainMod, Z, workspace, 1

bind = $mainMod, X, workspace, 2

bind = $mainMod, 3, workspace, 3

bind = $mainMod, 4, workspace, 4

bind = $mainMod, 5, workspace, 5

bind = $mainMod, 6, workspace, 6

bind = $mainMod, 7, workspace, 7

bind = $mainMod, 8, workspace, 8

bind = $mainMod, 9, workspace, 9

bind = $mainMod, 0, workspace, 10


# Move active window to a workspace with mainMod + SHIFT + [0-9]

bind = $mainMod SHIFT, 1, movetoworkspace, 1

bind = $mainMod SHIFT, 2, movetoworkspace, 2

bind = $mainMod SHIFT, 3, movetoworkspace, 3

bind = $mainMod SHIFT, 4, movetoworkspace, 4

bind = $mainMod SHIFT, 5, movetoworkspace, 5

bind = $mainMod SHIFT, 6, movetoworkspace, 6

bind = $mainMod SHIFT, 7, movetoworkspace, 7

bind = $mainMod SHIFT, 8, movetoworkspace, 8

bind = $mainMod SHIFT, 9, movetoworkspace, 9

bind = $mainMod SHIFT, 0, movetoworkspace, 10


# Example special workspace (scratchpad)

  bind = $mainMod, S, exec, ~/.config/hypr/minimize.sh

# Scroll through existing workspaces with mainMod + scroll

bind = $mainMod, mouse_down, workspace, e+1

bind = $mainMod, mouse_up, workspace, e-1


# Move/resize windows with mainMod + LMB/RMB and dragging

bindm = $mainMod, mouse:272, movewindow

bindm = $mainMod, mouse:273, resizewindow


# Laptop multimedia keys for volume and LCD brightness

bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+

bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-

bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+

bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-


# Requires playerctl

bindl = , XF86AudioNext, exec, playerctl next

bindl = , XF86AudioPause, exec, playerctl play-pause

bindl = , XF86AudioPlay, exec, playerctl play-pause

bindl = , XF86AudioPrev, exec, playerctl previous

# --- Original Window Rules (Unmodified) ---

windowrule {
  name = windowrule-1
  no_focus = on
  match:class = ^$
  match:title = ^$
  match:xwayland = 1
  match:float = 1
  match:fullscreen = 0
  match:pin = 0
}

windowrule {
  name = windowrule-2
  focus_on_activate = on
  match:class = ^(jetbrains-.*)$
}

windowrule {
  name = windowrule-3
  dim_around = on
  center = on
  match:class = ^(jetbrains-.*)$
  match:float = 1
  match:title = ^(?!win)
}

windowrule {
  name = intellij-1
  focus_on_activate = on
  match:class = ^(jetbrains-.*)$
}

# search dialog
windowrule {
  name = intellij-2
  dim_around = on
  center = on
  match:class = ^(jetbrains-.*)$
  match:float = 1
  match:title = ^(?!win)
}

# autocomplete & menus
windowrule {
  name = intellij-3
  no_anim = on
  no_initial_focus = on
  rounding = 0
  match:class = ^(jetbrains-.*)$
  match:title = ^(win.*)$
}


windowrule {
  name = kitty
  float = on
  size = 800 500
  center = on
  no_initial_focus = off
  opacity = 0.8 override 0.6 override
  match:class = (kitty)
}
source = /home/shxzu/.config/hypr/noctalia/noctalia-colors.conf

